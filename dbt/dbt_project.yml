name: airflow_dbt_lab

version: "1.0.0"

config-version: 2

profile: airflow_dbt_lab

target-path: /tmp/dbt-target
log-path: /tmp/dbt-logs

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  airflow_dbt_lab:
    +materialized: table